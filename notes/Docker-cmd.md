# Docker commands

## Make docker work

```shell
docker compose up
```

## Close Docker

```shell
docker compose down
```

## Open psql CLI in Docker

```shell
docker compose exec -it db psql -U baloo -d lenslocked
```
